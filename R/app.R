#!/usr/bin/env R

# app.R
# runs `databike`

load.data()
load.uscorestart()
load.functions()
#log_errors()

# main app # maintain bcond > 0, also show score.... ... 
while(bcond > 0){
  
}
message("process finished")
# end options endgame ...
# play again?
# other stuff